export const baseURL = import.meta.env.VITE_APP_API_BASE_URL;
export const fallbackLocale = import.meta.env.VITE_APP_I18N_FALLBACK_LOCALE;
export const appVersion = import.meta.env.VITE_APP_VERSION;

export default {
  baseURL,
  fallbackLocale,
  appVersion,
};
