# Requirements

LabelIt can be deploy in a Kubernetes cluster using Helm. To deploy it, you need:

- A Kubernetes cluster
- [Helm](https://helm.sh/docs/intro/install/) installed in your local system
- A running Postgres database
